apiVersion : v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app  : echoserv
    comp : neto
spec:
  containers:
    - name : container-helloworld
      image: gcr.io/kubernetes-e2e-test-images/echoserver:2.2
      ports:
        - containerPort: 8080
