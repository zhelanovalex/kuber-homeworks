apiVersion: v1
kind: Service
metadata:
  name: netology-svc
  labels:
     app  : echoneto
spec:
  selector:
    app: echoneto
  ports:
    - name      : echoneto-listener
      protocol  : TCP
      port      : 10443
      targetPort: 8080
